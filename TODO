- Resolve conflicts
- Idea: Handle file permissions (replicate the file structure with the permissions? using sqlite with path->permissions?)
- open/release of history improved (leeping in memory for all reads)
- confirm usage of .gitignore to omit files
